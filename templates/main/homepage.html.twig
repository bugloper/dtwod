{%  extends 'base.html.twig' %}

{%  block title  %} Home {%  endblock %}

{%  block body %}
<h1>Homepage: Hello World!</h1>
<div>
    No of StarShips: {{ starShips|length }}
    {% if starShips|length > 69 %}
        {# Comment #}
        <p>Dang Ship: {{ 420 }}</p>
    {% endif %}
    <p>My Ship: {{ myShip.name }}</p>
</div>
{% endblock %}